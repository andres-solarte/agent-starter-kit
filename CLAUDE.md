# CLAUDE.md

Claude Code entry for **agent-starter-kit** process development.

1. Skills: `.claude/skills/` → `.agents/skills/` (SoT).
2. Rules: `.claude/rules/` → `.agents/rules/` (SoT).
3. Product memory protocol (when using the template): `agent-knowledge-template/AGENTS.md`.

Prefer `/ask-question` triage and the `ask-*` skills. Do not improvise unanchored process — follow `ask-agent-skill-discipline`.
