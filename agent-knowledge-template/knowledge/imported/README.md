# imported/

Docs brought in from sibling repos via `/ask-centralize-docs` (or the install scan step).

- Map: [IMPORT-MAP.md](./IMPORT-MAP.md)
- Prefer promoting mature content into `product/`, `architecture/`, `domain/`, etc., then leave a note in the import map.
