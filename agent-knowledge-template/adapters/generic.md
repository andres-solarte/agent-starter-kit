# Adapter — generic (Codex, Copilot, Windsurf, …)

1. Ensure the agent can read this git repo.  
2. Instruct it (or rely on native `AGENTS.md` support) to follow [`../AGENTS.md`](../AGENTS.md).  
3. No tool-specific folder required inside this repo beyond optional short files under `adapters/`.
