# Session backlog

Personal parked ideas, bugs, or requests (`/ask-backlog` + rule `ask-focus-scope`).

Path: `users/<email>/session-backlog.md` (tracked — can commit with agent-knowledge).

## Open

## Done / discarded
